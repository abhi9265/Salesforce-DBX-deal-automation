# CI repair note

This file records the reliability milestone for the repository's CI process.

CI is expected to execute on feature pushes and pull requests. The test suite is the merge gate; failures are fixed in code rather than bypassed.
